# Archived Gallery drawings

`second-offset-arrangement-2026-08-22.svg` is the original second-offset
arrangement illustration, first committed in `26f8938`. It is preserved for
comparison only and is not an input to Gallery generation.

The maintained replacement is `../gallery-package-title-second-offset-arrangement.svg`.
